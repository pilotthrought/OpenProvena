// Configuration PostCSS pour OpenProvena
// Optimisation du CSS pour la production

module.exports = {
  plugins: {
    // Autoprefixer pour la compatibilité des navigateurs
    autoprefixer: {},
    // Tailwind CSS pour la génération du CSS
    tailwindcss: {},
  },
};
